echo "run as root to run";sleep 5
apt install dialog chromium breeze-icon-theme kdenlive unzip openssh-server vim
echo "if packages don't work, run apt -f install"
echo "if everything works YOU ARE DONE!!"
